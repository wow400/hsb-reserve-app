# HSB Reserve App

Single Cloudflare Worker application.

It serves:
- HSB Reserve Calculator UI
- `/api/status` Aviationstack flight-status endpoint

Required Cloudflare secret:

`AVIATIONSTACK_KEY`

Do not put the Aviationstack key into GitHub.
